/**
 * Remotion configuration file.
 *
 * In Remotion v4 the Config.set*() helpers were removed.
 * Render settings (codec, pixel format, concurrency) are now passed
 * directly to renderMedia() in scripts/render.ts.
 *
 * This file is kept as a placeholder for future Remotion config needs.
 */
import {Config} from 'remotion';

// Remotion v4: Config is currently an empty namespace.
// All render options are configured in scripts/render.ts via renderMedia().
void Config;
